name: 'Launch a compute instance'
hosts: localhost
tasks:
    name: 'Launch new VM'
    os_server: { state: present, network: LOCAL-NET, image: 'CentOS 7', name: vm005, flavor: std.1, region_name: RegionTwo }

